# 服务器部署策略
server:
  servlet:
    context-path: /app
  port: 8888

# Spring 日志策略
logging:
  level:
    root: info

# 启用热部署
#spring:
#  # 编译时默认启用 dev 环境，运行时切换环境命令: -Dspring.profiles.active=dev
#  profiles:
#    active: dev

spring:
  datasource:
    url: jdbc:mysql://39.108.12.197:3306/app-demo?useUnicode=true&characterEncoding=utf8
#    url: jdbc:mysql:/localh:3306/app-demo?useUnicode=true&characterEncoding=utf8
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver


mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
